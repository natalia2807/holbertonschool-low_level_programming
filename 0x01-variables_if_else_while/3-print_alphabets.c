#include <stdio.h>
int main(void)
{
  char A[25] = "abcdefghijklmnñopqrstuvwxyz";
  char ch = 'G';
  for (int i = 0; i< 25; i++)
    {
      putchar(A[I]);
    }
  for (int i = 0; i < 25; i++);
  {
    putchar (toupper(A[i]));
  }
  return (0);
}
