#include "holberton.h"
/**
 * _isupper - function that checks for uppercase character.
 * @c: input value to check
 * Return: int.
 */
int _isdigit(int c)
{
if (c >=0 && c <= 9)
{
return (1);
}
else
return (0);
} 