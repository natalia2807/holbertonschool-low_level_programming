#include "holberton.h"

/**
 * rev_string - reverses a string
 * @s: string to reverse
 *
 * Return: void
 */
void puts2(char *str)
{
	str = 1;
    if(*(str >=0 && str<=10))
    {
    putchar (str);
    }
    putchar ('\n');

}